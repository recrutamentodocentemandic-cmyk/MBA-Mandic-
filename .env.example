# Token do bot criado no @BotFather (Rodrigo guarda; nunca commitar)
TELEGRAM_BOT_TOKEN=

# Chave da API OpenAI (platform.openai.com → API keys)
OPENAI_API_KEY=

# Chat IDs — o bot responde /chatid em qualquer chat (ver README)
# CURATOR_CHAT_ID: chat privado do Rodrigo (curadoria em modo sombra)
# GROUP_CHAT_ID: grupo dos alunos do MBA
# MGMT_CHAT_ID: grupo do time de gestão (lembretes + relatório semanal)
CURATOR_CHAT_ID=
GROUP_CHAT_ID=
MGMT_CHAT_ID=

# Modelos (roteamento: mini p/ classificação, principal p/ respostas e agente)
CLASSIFIER_MODEL=gpt-5-mini
ANSWER_MODEL=gpt-5.1

# Caminho do banco SQLite (no Railway, montar um volume em /data)
DB_PATH=./data/bot.db
